package com.example.demo.enums;

public enum AccountStatus {
    CREATED,    // Account is created but not yet activated
    ACTIVATED,  // Account is active and operational
    SUSPENDED,  // Account is suspended and not operational
    CLOSED      // Account is close
    }