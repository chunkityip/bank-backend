package com.example.demo.enums;

public enum OperationType {
    DEPOSIT,    // Represents money being deposit to another
    TRANSFER,   // Represents money being transferred from one account to another
    WITHDRAWAL  // Represents money being withdrawn from an account
}
